# Deployment-Workflows

Hier kommt später das Deployment auf den Webserver hinein.

Der Webserver wird bewusst erst im nächsten Schritt verbunden. Zugangsdaten werden nicht im Repository gespeichert, sondern ausschließlich als GitHub-Secrets hinterlegt.
